# WARNING: VITE_ variables are bundled into client-side JavaScript and are publicly visible.
# Only include public keys here (e.g. Supabase anon key), never service-role or secret keys.
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
